 /*
  *= require bootstrap.min
  *= require_self
  */

@font-face {
  font-family: 'Glyphicons Halflings';
  src: url(<%=font_path('glyphicons-halflings-regular.eot')%> );
  src: url(<%=font_path('glyphicons-halflings-regular.eot?#iefix')%>) format('embedded-opentype'),
  url(<%=font_path('glyphicons-halflings-regular.woff')%>) format('woff'),
  url(<%=font_path('glyphicons-halflings-regular.ttf')%>) format('truetype'),
  url(<%=font_path('glyphicons-halflings-regular.svg#glyphicons_halflingsregular')%>) format('svg');
}

 .container.settings {
   width: 600px;
   margin-top: 30px;
 }
 .container {
   /*margin-top: 30px;*/
   width: 100%;
   .alert {
     display: inline-block;
     padding: 4px;
     margin: 4px;
   }
 }

 .categories {
   margin: 4px;
   padding: 0;
   width: 100%;
   li {
     list-style: none;
     padding: 4px 20px;
     border-radius: 4px;
      width: 100%;
     &:hover {
       background-color: #bce8f1;
     }
     h3.name {
     }
     [data-method] {
       i  {
        margin: 4px;
       }
     }
   }
 }
